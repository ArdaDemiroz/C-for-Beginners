using System;

class Program {
    static void Main() {
        bool isRaining = false;
        bool isCold = true;
        if (isRaining || isCold) {
            Console.WriteLine("Better to stay inside.");
        }
    }
}